package com.foreverspark.logicsim.client.screen.v2;

/** Replace one selected reusable CHIP instance with the currently chosen library CHIP. */
public interface CustomChipReplacementAccess {
    boolean logic$replaceSelectedWithPlacementChip();
}
