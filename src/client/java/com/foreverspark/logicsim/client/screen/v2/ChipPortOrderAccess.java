package com.foreverspark.logicsim.client.screen.v2;

/** Explicit reusable-CHIP interface ordering for selected root INPUT/OUTPUT terminals. */
public interface ChipPortOrderAccess {
    boolean logic$moveSelectedChipPort(int direction);
}
